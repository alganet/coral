sudo: required
language: bash
services:
  - docker

before_install:
  # Update brew catalogue if going to install anything
  - ${BREW:+brew update}

  # Install brew dependencies
  - ${BREW:+brew install ${BREW}}

  # Build spec_doc module
  - ${spec_doc_shell} ./script/dev module_assemble spec_doc spec_doc

script:
  - spec_doc_shell=${spec_doc_shell} ${spec_doc_shell} ./spec_doc spec_doc $(find ${TARGET})

matrix:
  include:
    - env: spec_doc_shell=sh TARGET="TESTING.md"
      os:  linux

    - env: spec_doc_shell="bash" TARGET=". -name 'README.md'"
      os:  osx

    - env: spec_doc_shell="zsh" BREW="zsh" TARGET=". -name 'README.md'"
      os:  osx

    - env: spec_doc_shell="ksh" BREW="ksh" TARGET=". -name 'README.md'"
      os:  osx

    - env: spec_doc_shell="mksh" BREW="mksh" TARGET=". -name 'README.md'"
      os:  osx

    - env: spec_doc_shell="dash" BREW="dash" TARGET=". -name 'README.md'"
      os:  osx
